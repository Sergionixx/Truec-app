import React from "react";
import { useModalFocus } from "./useModalFocus";
interface Props { isOpen: boolean; onClose: () => void; showToast: (message: string, type: "success" | "error" | "info" | "warning") => void; onOpenPublish: () => void; onOpenTrades: () => void; onOpenPlayStore: () => void; }
export const EvaluatorGuideModal = ({isOpen,onClose,onOpenPlayStore}: Props) => {
 const ref=useModalFocus(isOpen,onClose);
 if(!isOpen) return null;
 return <div ref={ref} role="dialog" aria-modal="true" aria-labelledby="guide-title" className="fixed inset-0 z-50 bg-slate-900/80 p-4 flex items-center justify-center"><section className="bg-white rounded-2xl p-6 max-w-xl max-h-[90dvh] overflow-auto"><h2 id="guide-title" className="font-bold text-xl">Guía de demostración</h2><ol className="list-decimal pl-5 space-y-3 my-5"><li>Inicia sesión con demo@truec.app / demo123. El servidor debe estar activo.</li><li>Publica un artículo; revisa sus datos, edítalo y retíralo.</li><li>Selecciona un producto que admita trueque y envía una propuesta.</li><li>Entra con vendedor@truec.app / demo123 para responder propuestas recibidas.</li><li>Consulta una subasta, intenta una puja insuficiente y después una válida.</li><li>En Android, registra un usuario y comprueba el guardado local con Room. Abre el catálogo desde el formulario.</li></ol><p>La web requiere servidor para guardar; Android funciona localmente. Las cuentas y productos iniciales son ejemplos.</p><div className="flex gap-3 mt-5"><button onClick={onClose} className="p-3 border rounded">Cerrar guía</button><button onClick={()=>{onClose();onOpenPlayStore();}} className="p-3 bg-teal-700 text-white rounded">Simular publicación</button></div></section></div>;
};
